table! {
    activity (id) {
        id -> Text,
        timestamp -> Text,
        data_type -> Text,
        data_type_version -> Integer,
        sampler -> Text,
        sampler_sequence_id -> Text,
        data -> Text,
    }
}
